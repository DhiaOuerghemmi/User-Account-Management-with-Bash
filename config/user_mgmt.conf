#--- User account lifecycle parameters
PASS_MAX_DAYS=90            # days before password rotation
INACTIVE_DAYS=30            # days of inactivity before deletion
ADMIN_EMAIL=dhiaouerghemmi1@gmail.com

#--- Logging
LOG_DIR=/var/log/user_mgmt
LOG_RETENTION_DAYS=60

#--- Email (mailx) settings
MAIL_SUBJECT_PREFIX="[UserMgmt]"
MAIL_CMD=mailx

#--- Lockfile
LOCKFILE=/var/lock/user_mgmt.lock
